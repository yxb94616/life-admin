import { MenuUnfoldOutlined } from "@ant-design/icons";

const CollapseIcon = () => {
	return (
		<div className="mr-5 text-lg cursor-pointer transition-colors duration-300 flex items-center justify-center">
			<MenuUnfoldOutlined />
		</div>
	);
};

export default CollapseIcon;
